<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- 加载属性文件 -->
	<!-- <properties resource="db.properties"></properties> -->
	<!-- 给包中的类注册别名,注册后可以直接使用类名,而不用使用全限定的类名(就是不用包含包名)。 -->
	<!-- <typeAliases>
		<package name="com.library.model"/>
	</typeAliases>
	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" />
			<dataSource type="POOLED">
				<property name="driver" value="${jdbc.driver}" />
				<property name="url" value="${jdbc.url}" />
				<property name="username" value="${jdbc.username}" />
				<property name="password" value="${jdbc.password}" />
			</dataSource>
		</environment>
	</environments> -->
	<!-- <mappers>
	   <mapper resource="com/library/model/sqlmap/libraryInfoMapper.xml"/>
	   <mapper resource="com/library/model/sqlmap/parameterInfoMapper.xml"/>
	   <mapper resource="com/library/model/sqlmap/bookcaseMapper.xml"/>
	   <mapper resource="com/library/model/sqlmap/managerMapper.xml"/>
	   <mapper resource="com/library/model/sqlmap/publishMapper.xml"/>
	   <mapper resource="com/library/model/sqlmap/readerTypeMapper.xml"/>
	   <mapper resource="com/library/model/sqlmap/bookTypeMapper.xml"/>
	   <mapper resource="com/library/model/sqlmap/readerMapper.xml"/>
	</mappers> -->
</configuration>